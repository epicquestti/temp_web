import { styled } from "@mui/material/styles";
import { DefaultHeader } from "./DefaultHeader";
export const LeftHeader = styled(DefaultHeader)(() => ({
  borderTopLeftRadius: "4px",
  borderBottomLeftRadius: "4px",
}));
